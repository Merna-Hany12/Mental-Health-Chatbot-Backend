receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317

processors:
  batch:
    timeout: 5s
    send_batch_size: 512

exporters:
  otlp_http/axiom:
    endpoint: "https://api.axiom.co"
    headers:
      authorization: "Bearer ${env:AXIOM_TOKEN}"
      x-axiom-dataset: "${env:AXIOM_DATASET}"

  debug:
    verbosity: normal

service:
  pipelines:
    logs:
      receivers:  [otlp]
      processors: [batch]
      exporters:  [otlp_http/axiom, debug]
    metrics:
      receivers:  [otlp]
      processors: [batch]
      exporters:  [otlp_http/axiom, debug]